{application, async, [
    {description, "Async Monad"},
    {vsn, "0.6.7"},
    {modules, []}, 
    {mod, {async_app, ["%MAKETIME%"]}},
    {registered, [async_sup, async_worker_sup, async_channel_sup]},
    {applications, [kernel, stdlib, erlando]},
    {env, []}
]}.
