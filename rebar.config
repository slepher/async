%% -*- erlang -*-
{erl_opts, [debug_info]}.
{cover_enabled, true}.

{provider_hooks, [{post, [{compile, {erlando, compile}}]}]}.

{plugins, [
        {rebar3_erlando,
         {git, "https://github.com/slepher/rebar3_erlando.git", {tag, "0.3.0"}}}
       ]}.

{deps, [
        {'astranaut', {git, "https://github.com/slepher/astranaut.git", {tag, "0.12.0"}}},
        {'erlando',   {git, "https://github.com/slepher/erlando.git",   {tag, "2.11.2"}}},
        {'lenses',    {git, "https://github.com/slepher/lenses.git",    {tag, "0.7.1"}}}
       ]}.

{xref_checks, [undefined_function_calls, undefined_functions,
               locals_not_used,
               deprecated_function_calls, deprecated_functions]}.

{dialyzer,
 [
  %% Store PLT locally inside the project in .rebar (Default)
  {plt_location, local},
  {warnings, [unmatched_returns, error_handling, unknown]}
 ]}.

{profiles, [
  {test, [
          {erl_opts, [{parse_transform, astranaut_disable_tco}, nowarn_export_all]}
  ]}
]}.

{docker_ci, [
  {erlang_versions, ["21", "23", "28", "29"]},
  {run_xref, true},
  {run_dialyzer, false},
  {use_checkouts, auto},
  {output_lang, auto},
  {log_port, 8081}
]}.
